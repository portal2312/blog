214에 접속 후 아래 명령 실행.
ssh -D *:7790 -p 7789 localhost
pw : netcruz!1

이후 ssh 접속 시에는 214서버에서 아래 명령 수행.
ssh -p 7789 localhost
pw : netcruz!1

웹 접속시에는 프록시 설정을 214서버의 7790 포트를 바라보게 설정 후
웹 주소는 211.115.204.129 로 들어가면 됩니다.

(참고로 214에 티먹스 shlim 계정으로 이미 셋팅했습니다.)